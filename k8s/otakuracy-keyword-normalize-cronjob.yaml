apiVersion: batch/v1
kind: CronJob
metadata:
  labels:
    app: otakuracy
  name: otakuracy-keyword-normalize
  namespace: default
spec:
  concurrencyPolicy: Forbid
  failedJobsHistoryLimit: 3
  successfulJobsHistoryLimit: 3
  schedule: "30 10 * * *"
  suspend: false
  jobTemplate:
    spec:
      activeDeadlineSeconds: 3600
      backoffLimit: 1
      template:
        spec:
          hostNetwork: true
          restartPolicy: OnFailure
          containers:
          - name: keyword-normalize
            image: localhost/otakuracy-pipeline:latest
            imagePullPolicy: Never
            workingDir: /app
            command:
            - /app/.venv/bin/python
            - app/src/normalize_keywords.py
            env:
            - name: PYTHONPATH
              value: /app/src:/common/src
            - name: CLAUDE_GATEWAY_URL
              value: http://127.0.0.1:18080
            volumeMounts:
            - mountPath: /data
              name: data
          volumes:
          - name: data
            hostPath:
              path: /srv/otakuracy/data
              type: Directory
